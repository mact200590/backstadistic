// import mysql from "mysql";
// import Knex from "knex";
// const { DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_PORT,DB_CLIENT } = process.env;
// // Deprecated!!!! Please, do not use this, only use createConnection to inject it to Context
// // export const pool = mysql.createPool({
// //   host: DB_HOST,
// //   user: DB_USER,
// //   password: DB_PASSWORD,
// //   database: DB_NAME,
// //   port: DB_PORT ? Number.parseInt(DB_PORT) : undefined
// // });

// const knex= require('knex')({
//     client: DB_CLIENT,
//     connection: {
//       host :DB_HOST,
//       user : DB_USER,
//       password : DB_PASSWORD,
//       database : DB_NAME
//     }
//   });

  // export default knex;







