import { UniversalLoader, SingleFileOptions } from '@graphql-toolkit/common';
import { GraphQLTagPluckOptions } from '@graphql-toolkit/graphql-tag-pluck';
declare type GitLoaderOptions = SingleFileOptions & {
    pluckConfig: GraphQLTagPluckOptions;
};
export declare class GitLoader implements UniversalLoader {
    loaderId(): string;
    canLoad(pointer: string): Promise<boolean>;
    canLoadSync(pointer: string): boolean;
    load(pointer: string, options: GitLoaderOptions): Promise<import("../../../common/src").Source | {
        location: string;
        rawSDL: string;
    }>;
    loadSync(pointer: string, options: GitLoaderOptions): import("../../../common/src").Source | {
        location: string;
        rawSDL: string;
    };
}
export {};
